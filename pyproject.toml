[project]
name = "chess"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.6.0",
    "pytest>=9.1.1",
    "ruff>=0.15.12",
]

[tool.uv]
package = true